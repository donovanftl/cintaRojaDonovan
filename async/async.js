// const hola = (nombre) => {
//     return `Cómo estas, ${nombre }?`;
    
// };

// const adios = (name) => {
//     return `Hasta luego, ${name}`;    
// };

// const ordenSuperior = (callback, name) => {
//        return callback(name) 
// };

// console.log(ordenSuperior(hola, 'Donovan'));


    // const suma = (num1,  num2) => {
    //         return num1 + num2;
    // };

    // const resta = (num1,  num2) => {
    //     return num1 - num2;
    // };

    // const multiplica = (num1,  num2) => {
    //     return num1 * num2;
    // };

    // const divide = (num1,  num2) => {
    //     return num1 / num2;
    // }
    // const ordenSuperior = (callback, num1, num2) => {
    //     return callback (num1, num2)

    // }
    // console.log(ordenSuperior(multiplica, 20,2))

    const alumnos = [7,8,9,5,6]
    const alumnos2 = []
    const media = (calif) => {
        let acc = 0;
    for (let i = 0; i < calif.length; i++){
        acc +- calif
    }
    }